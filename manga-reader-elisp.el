(defun manga-reader (folder)
  "Opens subsequent chapters of hte manga in FOLDER for reading"
  (interactive)
  (message "cash money"))

(defun multiply-by-seven (number)
  "Multiplies NUMBER by seven and returns the result"
  (interactive)
  (message "7 * %d = %d" number (* 7 number)))

(multiply-by-seven 12)


(message "7 * %d = %d" number  (* 7 number))





